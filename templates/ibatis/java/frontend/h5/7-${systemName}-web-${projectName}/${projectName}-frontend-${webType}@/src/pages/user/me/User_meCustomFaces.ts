/**
 *  Do not remove this unless you get business authorization.
 *  User_me
 *  init by [stategen.progen] ,can be edit manually ,keep when "keep this"
 *  由 [stategen.progen]代码生成器初始化，可以手工修改,但如果遇到 keep this ,请保留导出设置以备外部自动化调用
 */
import {Effect, Effects, Reducers, IModel, BaseState, modelPathsProxy, ConnectionPros, Subscriptions, Reducer, AreaState, mergeObjects} from '@utils/DvaUtil';
import User from "@i/beans/User";
import {User_meState} from '@i/interfaces/User_meFaces';

/*** keep this export */
export const user_meCustomState = {

}

/*** keep this export */
export interface User_meCustomSubscriptions extends Subscriptions {

}

/*** keep this export */
export interface User_meCustomEffects extends Effects {

}

/*** keep this export */
export interface User_meCustomReducers extends Reducers<User_meState> {

}
