<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sqlMap PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN" 
    "http://ibatis.apache.org/dtd/sql-map-2.dtd">
<!-- 本文件由dalgen生成，任何在本文件上的手工修改将会在下次生成时被覆盖 -->
<sqlMap>


    <resultMap id="RoleMenu.${systemName}.RM" class="${packageName}.domain.RoleMenu">
        <result property="id" column="id" javaType="java.lang.Long" jdbcType="BIGINT"  />
        <result property="roleId" column="role_id" javaType="java.lang.String" jdbcType="VARCHAR"  />
        <result property="menuId" column="menu_id" javaType="java.lang.Long" jdbcType="BIGINT"  />
        <result property="createTime" column="create_time" javaType="java.util.Date" jdbcType="TIMESTAMP"  />
        <result property="updateTime" column="update_time" javaType="java.util.Date" jdbcType="TIMESTAMP"  />
        <result property="deleteFlag" column="delete_flag" javaType="java.lang.Integer" jdbcType="INTEGER"  />
    </resultMap>
    

	
	
    
	<insert id="insert.RoleMenu.${systemName}">
		insert into role_menu (
            create_time
            , update_time
            , delete_flag
            <isNotNull prepend="," property="id"> id </isNotNull>
            <isNotNull prepend="," property="roleId"> role_id </isNotNull>
            <isNotNull prepend="," property="menuId"> menu_id </isNotNull>
            ) VALUES (CURRENT_TIMESTAMP(6),CURRENT_TIMESTAMP(6),0 
            <isNotNull prepend="," property="id"> #id# </isNotNull> 
            <isNotNull prepend="," property="roleId"> #roleId# </isNotNull> 
            <isNotNull prepend="," property="menuId"> #menuId# </isNotNull>)
            <selectKey keyProperty="id" resultClass="long">
                select  LAST_INSERT_ID()
            </selectKey>
	</insert>
	
	<update id="delete.RoleMenu.${systemName}">
		UPDATE role_menu SET
            delete_flag = 1
            , update_time = CURRENT_TIMESTAMP(6)
            where
            delete_flag = 0
            and id = #id#
	</update>
	
    
	
	<update id="update.RoleMenu.${systemName}">
		UPDATE role_menu
            SET update_time= CURRENT_TIMESTAMP(6)
            <dynamic>
                <isNotNull prepend="," property="roleId">
                    role_id =  #roleId#
                </isNotNull>
                <isNotNull prepend="," property="menuId">
                    menu_id =  #menuId#
                </isNotNull>
            </dynamic>
            where
            delete_flag = 0
            and id = #id#
	</update>
	
    
	
	<select id="getRoleMenuById.RoleMenu.${systemName}" resultMap="RoleMenu.${systemName}.RM" >
    	select
 /* getRoleMenuById.RoleMenu.${systemName} */             a.id,
            a.role_id,
            a.menu_id,
            a.create_time,
            a.update_time,
            a.delete_flag
            from role_menu a
            where
            a.delete_flag = 0
            and a.id = #id#
	</select>	

	    
	
	
    
	
	<select id="getRoleMenuPageListByDefaultQuery.RoleMenu.${systemName}" resultMap="RoleMenu.${systemName}.RM" >
    	select
 /* getRoleMenuPageListByDefaultQuery.RoleMenu.${systemName} */             a.id,
            a.role_id,
            a.menu_id,
            a.create_time,
            a.update_time,
            a.delete_flag
            from role_menu a
            where
            a.delete_flag = 0
            <isNotEmpty open="and" property="ids">
                a.id in
                <iterate close=")" conjunction="," open="(" property="ids">
                    #ids[]#
                </iterate>
            </isNotEmpty>
            <isNotEmpty open="and" property="roleIds">
                a.role_id in
                <iterate close=")" conjunction="," open="(" property="roleIds">
                    #roleIds[]#
                </iterate>
            </isNotEmpty>
            <isNotEmpty open="and" property="menuIds">
                a.menu_id in
                <iterate close=")" conjunction="," open="(" property="menuIds">
                    #menuIds[]#
                </iterate>
            </isNotEmpty>
            <isNotNull open="and" property="createTimeMin">
                a.create_time >=#createTimeMin#
            </isNotNull>
            <isNotNull open="and" property="createTimeMax">
                a.create_time &lt;#createTimeMax#
            </isNotNull>
            <isNotNull open="and" property="updateTimeMin">
                a.update_time >=#updateTimeMin#
            </isNotNull>
            <isNotNull open="and" property="updateTimeMax">
                a.update_time &lt;#updateTimeMax#
            </isNotNull>
            <isEmpty property="ids">
            <isEmpty property="roleIds">
            <isEmpty property="menuIds">
            <isNull property="createTimeMin">
            <isNull property="createTimeMax">
            <isNull property="updateTimeMin">
            <isNull property="updateTimeMax">
              and 0 = 1
            </isNull>
            </isNull>
            </isNull>
            </isNull>
            </isEmpty>
            </isEmpty>
            </isEmpty>
			limit #offset#,#limit#
	</select>	

	<select id="getRoleMenuPageListByDefaultQuery.RoleMenu.${systemName}.count" resultClass="long" >
    	select  /* getRoleMenuPageListByDefaultQuery.RoleMenu.${systemName}.count */ count(*)  from role_menu a
            where
            a.delete_flag = 0
            <isNotEmpty open="and" property="ids">
                a.id in
                <iterate close=")" conjunction="," open="(" property="ids">
                    #ids[]#
                </iterate>
            </isNotEmpty>
            <isNotEmpty open="and" property="roleIds">
                a.role_id in
                <iterate close=")" conjunction="," open="(" property="roleIds">
                    #roleIds[]#
                </iterate>
            </isNotEmpty>
            <isNotEmpty open="and" property="menuIds">
                a.menu_id in
                <iterate close=")" conjunction="," open="(" property="menuIds">
                    #menuIds[]#
                </iterate>
            </isNotEmpty>
            <isNotNull open="and" property="createTimeMin">
                a.create_time >=#createTimeMin#
            </isNotNull>
            <isNotNull open="and" property="createTimeMax">
                a.create_time &lt;#createTimeMax#
            </isNotNull>
            <isNotNull open="and" property="updateTimeMin">
                a.update_time >=#updateTimeMin#
            </isNotNull>
            <isNotNull open="and" property="updateTimeMax">
                a.update_time &lt;#updateTimeMax#
            </isNotNull>
            <isEmpty property="ids">
            <isEmpty property="roleIds">
            <isEmpty property="menuIds">
            <isNull property="createTimeMin">
            <isNull property="createTimeMax">
            <isNull property="updateTimeMin">
            <isNull property="updateTimeMax">
              and 0 = 1
            </isNull>
            </isNull>
            </isNull>
            </isNull>
            </isEmpty>
            </isEmpty>
            </isEmpty>
	</select>
	    
	
	
    
	
	<select id="getRoleMenusByIds.RoleMenu.${systemName}" resultMap="RoleMenu.${systemName}.RM" >
    	select
 /* getRoleMenusByIds.RoleMenu.${systemName} */             a.id,
            a.role_id,
            a.menu_id,
            a.create_time,
            a.update_time,
            a.delete_flag
            from role_menu a
            where
            a.delete_flag = 0
            and a.id in
            <iterate close=")" conjunction="," open="(" property="ids">
                #ids[]#
            </iterate>
	</select>	

	    
	
	
    
	
	<update id="deleteByIds.RoleMenu.${systemName}">
		UPDATE role_menu SET
               delete_flag = 1
               , update_time = CURRENT_TIMESTAMP(6)
            where
            delete_flag = 0
            and id in
            <iterate close=")" conjunction="," open="(" property="ids">
                #ids[]#
            </iterate>
	</update>
	
    
	
	<select id="getRoleMenusByMenuIds.RoleMenu.${systemName}" resultMap="RoleMenu.${systemName}.RM" >
    	select
 /* getRoleMenusByMenuIds.RoleMenu.${systemName} */             a.id,
            a.role_id,
            a.menu_id,
            a.create_time,
            a.update_time,
            a.delete_flag
            from role_menu a
            where
            <isNotEmpty property="menuIds">
                a.menu_id in
                <iterate close=")" conjunction="," open="(" property="menuIds">
                    #menuIds[]#
                </iterate>
            </isNotEmpty>
            and a.delete_flag = 0
	</select>	

	    
	
	
    

</sqlMap>

