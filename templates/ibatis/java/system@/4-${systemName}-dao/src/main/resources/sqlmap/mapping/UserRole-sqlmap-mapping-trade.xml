<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sqlMap PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN" 
    "http://ibatis.apache.org/dtd/sql-map-2.dtd">
<!-- 本文件由dalgen生成，任何在本文件上的手工修改将会在下次生成时被覆盖 -->
<sqlMap>


    <resultMap id="UserRole.${systemName}.RM" class="${packageName}.domain.UserRole">
        <result property="id" column="id" javaType="java.lang.Long" jdbcType="BIGINT"  />
        <result property="userId" column="user_id" javaType="java.lang.String" jdbcType="VARCHAR"  />
        <result property="roleId" column="role_id" javaType="java.lang.String" jdbcType="VARCHAR"  />
        <result property="roleType" column="role_type" javaType="java.lang.String" jdbcType="VARCHAR"  />
        <result property="createTime" column="create_time" javaType="java.util.Date" jdbcType="TIMESTAMP"  />
        <result property="updateTime" column="update_time" javaType="java.util.Date" jdbcType="TIMESTAMP"  />
        <result property="deleteFlag" column="delete_flag" javaType="java.lang.Integer" jdbcType="INTEGER"  />
    </resultMap>
    

	
	
    
	<insert id="insert.UserRole.${systemName}">
		insert into user_role (
            create_time
            , update_time
            , delete_flag
            <isNotNull prepend="," property="id"> id </isNotNull>
            <isNotNull prepend="," property="userId"> user_id </isNotNull>
            <isNotNull prepend="," property="roleId"> role_id </isNotNull>
            <isNotNull prepend="," property="roleType"> role_type </isNotNull>
            ) VALUES (CURRENT_TIMESTAMP(6),CURRENT_TIMESTAMP(6),0 
            <isNotNull prepend="," property="id"> #id# </isNotNull> 
            <isNotNull prepend="," property="userId"> #userId# </isNotNull> 
            <isNotNull prepend="," property="roleId"> #roleId# </isNotNull> 
            <isNotNull prepend="," property="roleType"> #roleType# </isNotNull>)
            <selectKey keyProperty="id" resultClass="long">
                select  LAST_INSERT_ID()
            </selectKey>
	</insert>
	
	<update id="deleteById.UserRole.${systemName}">
		UPDATE user_role SET
            delete_flag = 1
            , update_time = CURRENT_TIMESTAMP(6)
            where
            delete_flag = 0
            and id = #id#
	</update>
	
    
	
	<update id="update.UserRole.${systemName}">
		UPDATE user_role
            SET update_time= CURRENT_TIMESTAMP(6)
            <dynamic>
                <isNotNull prepend="," property="userId">
                    user_id =  #userId#
                </isNotNull>
                <isNotNull prepend="," property="roleId">
                    role_id =  #roleId#
                </isNotNull>
                <isNotNull prepend="," property="roleType">
                    role_type =  #roleType#
                </isNotNull>
            </dynamic>
            where
            delete_flag = 0
            and id = #id#
	</update>
	
    
	
	<select id="getUserRoleById.UserRole.${systemName}" resultMap="UserRole.${systemName}.RM" >
    	select
 /* getUserRoleById.UserRole.${systemName} */             a.id,
            a.user_id,
            a.role_id,
            a.role_type,
            a.create_time,
            a.update_time,
            a.delete_flag
            from user_role a
            where
            a.delete_flag = 0
            and a.id = #id#
	</select>	

	    
	
	
    
	
	<select id="getUserRolePageListByDefaultQuery.UserRole.${systemName}" resultMap="UserRole.${systemName}.RM" >
    	select
 /* getUserRolePageListByDefaultQuery.UserRole.${systemName} */             a.id,
            a.user_id,
            a.role_id,
            a.role_type,
            a.create_time,
            a.update_time,
            a.delete_flag
            from user_role a
            where
            a.delete_flag = 0
            <isNotEmpty open="and" property="ids">
                a.id in
                <iterate close=")" conjunction="," open="(" property="ids">
                    #ids[]#
                </iterate>
            </isNotEmpty>
            <isNotEmpty open="and" property="userIds">
                a.user_id in
                <iterate close=")" conjunction="," open="(" property="userIds">
                    #userIds[]#
                </iterate>
            </isNotEmpty>
            <isNotEmpty open="and" property="roleIds">
                a.role_id in
                <iterate close=")" conjunction="," open="(" property="roleIds">
                    #roleIds[]#
                </iterate>
            </isNotEmpty>
            <isNotEmpty open="and" property="roleTypes">
                a.role_type in
                <iterate close=")" conjunction="," open="(" property="roleTypes">
                    #roleTypes[]#
                </iterate>
            </isNotEmpty>
            <isNotNull open="and" property="createTimeMin">
                a.create_time >=#createTimeMin#
            </isNotNull>
            <isNotNull open="and" property="createTimeMax">
                a.create_time &lt;#createTimeMax#
            </isNotNull>
            <isNotNull open="and" property="updateTimeMin">
                a.update_time >=#updateTimeMin#
            </isNotNull>
            <isNotNull open="and" property="updateTimeMax">
                a.update_time &lt;#updateTimeMax#
            </isNotNull>
            <isEmpty property="ids">
            <isEmpty property="userIds">
            <isEmpty property="roleIds">
            <isEmpty property="roleTypes">
            <isNull property="createTimeMin">
            <isNull property="createTimeMax">
            <isNull property="updateTimeMin">
            <isNull property="updateTimeMax">
              and 0 = 1
            </isNull>
            </isNull>
            </isNull>
            </isNull>
            </isEmpty>
            </isEmpty>
            </isEmpty>
            </isEmpty>
			limit #offset#,#limit#
	</select>	

	<select id="getUserRolePageListByDefaultQuery.UserRole.${systemName}.count" resultClass="long" >
    	select  /* getUserRolePageListByDefaultQuery.UserRole.${systemName}.count */ count(*)  from user_role a
            where
            a.delete_flag = 0
            <isNotEmpty open="and" property="ids">
                a.id in
                <iterate close=")" conjunction="," open="(" property="ids">
                    #ids[]#
                </iterate>
            </isNotEmpty>
            <isNotEmpty open="and" property="userIds">
                a.user_id in
                <iterate close=")" conjunction="," open="(" property="userIds">
                    #userIds[]#
                </iterate>
            </isNotEmpty>
            <isNotEmpty open="and" property="roleIds">
                a.role_id in
                <iterate close=")" conjunction="," open="(" property="roleIds">
                    #roleIds[]#
                </iterate>
            </isNotEmpty>
            <isNotEmpty open="and" property="roleTypes">
                a.role_type in
                <iterate close=")" conjunction="," open="(" property="roleTypes">
                    #roleTypes[]#
                </iterate>
            </isNotEmpty>
            <isNotNull open="and" property="createTimeMin">
                a.create_time >=#createTimeMin#
            </isNotNull>
            <isNotNull open="and" property="createTimeMax">
                a.create_time &lt;#createTimeMax#
            </isNotNull>
            <isNotNull open="and" property="updateTimeMin">
                a.update_time >=#updateTimeMin#
            </isNotNull>
            <isNotNull open="and" property="updateTimeMax">
                a.update_time &lt;#updateTimeMax#
            </isNotNull>
            <isEmpty property="ids">
            <isEmpty property="userIds">
            <isEmpty property="roleIds">
            <isEmpty property="roleTypes">
            <isNull property="createTimeMin">
            <isNull property="createTimeMax">
            <isNull property="updateTimeMin">
            <isNull property="updateTimeMax">
              and 0 = 1
            </isNull>
            </isNull>
            </isNull>
            </isNull>
            </isEmpty>
            </isEmpty>
            </isEmpty>
            </isEmpty>
	</select>
	    
	
	
    
	
	<select id="getUserRolesByIds.UserRole.${systemName}" resultMap="UserRole.${systemName}.RM" >
    	select
 /* getUserRolesByIds.UserRole.${systemName} */             a.id,
            a.user_id,
            a.role_id,
            a.role_type,
            a.create_time,
            a.update_time,
            a.delete_flag
            from user_role a
            where
            a.delete_flag = 0
            and a.id in
            <iterate close=")" conjunction="," open="(" property="ids">
                #ids[]#
            </iterate>
	</select>	

	    
	
	
    
	
	<update id="deleteByIds.UserRole.${systemName}">
		UPDATE user_role SET
               delete_flag = 1
               , update_time = CURRENT_TIMESTAMP(6)
            where
            delete_flag = 0
            and id in
            <iterate close=")" conjunction="," open="(" property="ids">
                #ids[]#
            </iterate>
	</update>
	
    

</sqlMap>

