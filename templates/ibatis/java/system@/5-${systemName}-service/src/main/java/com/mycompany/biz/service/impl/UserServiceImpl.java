/*
 * Copyright (c) 2016 - 2116 All Rights Reserved.
 * Powered By [rapid-generator]
 */
package ${packageName}.service.impl;

import java.util.List;
import javax.annotation.Resource;

import org.stategen.framework.lite.PageList;
import org.stategen.framework.util.StringUtil;

import ${packageName}.dao.UserDao;
import ${packageName}.domain.User;
import ${packageName}.service.UserService;

/**
 * UserServiceImpl
 * <pre>
 * database table: user
 * database table comments: User
 * This file is generated by <tt>dalgen</tt>, a DAL (Data Access Layer)
 *
 * 新生成的类中的方法，只有在不同名的情况下才会被追加到该类下，
 * 因此该类可以修改任何部分
 * </pre>
 */
public class UserServiceImpl implements UserService {

    @Resource(name = "userDao")
    UserDao userDao;

    /**
     * 
     * @see ${packageName}.dao.UserDao#deleteByEmail
     * @see ${packageName}.service.UserService#deleteByEmail
     */
    @Override
    public Long deleteByEmail(String email) {
        return userDao.deleteByEmail(email);
    }

    /**
     * 
     * @see ${packageName}.dao.UserDao#update
     * @see ${packageName}.service.UserService#update
     */
    @Override
    public User update(User user) {
        return userDao.update(user);
    }

    /**
     * 
     * @see ${packageName}.dao.UserDao#getUserByUserId
     * @see ${packageName}.service.UserService#getUserByUserId
     */
    @Override
    public User getUserByUserId(String userId) {
        return userDao.getUserByUserId(userId);
    }

    /**
     * 
     * @see ${packageName}.dao.UserDao#getUserByEmail
     * @see ${packageName}.service.UserService#getUserByEmail
     */
    @Override
    public User getUserByEmail(String email) {
        return userDao.getUserByEmail(email);
    }

    /**
     * 
     * @see ${packageName}.dao.UserDao#getUserPageListByDefaultQuery
     * @see ${packageName}.service.UserService#getUserPageListByDefaultQuery
     */
    @Override
    public PageList<User> getUserPageListByDefaultQuery(User user, int pageSize, int pageNum) {
        return userDao.getUserPageListByDefaultQuery(user, pageSize, pageNum);
    }

    /**
     * 
     * @see ${packageName}.dao.UserDao#getUsersByUserIds
     * @see ${packageName}.service.UserService#getUsersByUserIds
     */
    @Override
    public List<User> getUsersByUserIds(java.util.List<String> userIds) {
        return userDao.getUsersByUserIds(userIds);
    }

    /**
     * 
     * @see ${packageName}.dao.UserDao#getUsersByEmails
     * @see ${packageName}.service.UserService#getUsersByEmails
     */
    @Override
    public List<User> getUsersByEmails(java.util.List<String> emails) {
        return userDao.getUsersByEmails(emails);
    }

    /**
     * 
     * @see ${packageName}.dao.UserDao#deleteByUserIds
     * @see ${packageName}.service.UserService#deleteByUserIds
     */
    @Override
    public List<String> deleteByUserIds(java.util.List<String> userIds) {
        return userDao.deleteByUserIds(userIds);
    }

    /**
     * 
     * @see ${packageName}.dao.UserDao#deleteByEmails
     * @see ${packageName}.service.UserService#deleteByEmails
     */
    @Override
    public Long deleteByEmails(java.util.List<String> emails) {
        return userDao.deleteByEmails(emails);
    }

    /**
     * 
     * @see ${packageName}.dao.UserDao#login
     * @see ${packageName}.service.UserService#login
     */
    @Override
    public User login(String username, String password) {
        return userDao.login(username, password);
    }

    /*** 保存user,有id时更新，没有id时插入,并带回新的id，返回 user*/
    @Override
    public User saveUser(User user) {
        if (user != null) {
            java.lang.String userId = user.getUserId();
            if (StringUtil.isBlank(userId)) {
                insert(user);
            } else {
                update(user);
            }
        }
        return user;
    }

    /*** 批量保存users,有id时更新，没有id时插入,并带回新的id，返回 users*/
    @Override
    public List<User> saveUsers(List<User> users) {
        for (User user : users) {
            this.saveUser(user);
        }
        return users;
    }

    /**
     * 
     * @see ${packageName}.dao.UserDao#insert
     * @see ${packageName}.service.UserService#insert
     */
    @Override
    public User insert(User user) {
        return userDao.insert(user);
    }

    /**
     * 
     * @see ${packageName}.dao.UserDao#delete
     * @see ${packageName}.service.UserService#delete
     */
    @Override
    public String delete(String userId) {
        return userDao.delete(userId);
    }
}
