/**
 *  Do not remove this unless you get business authorization.
 *  User_$userId
 *  init by [stategen.progen] ,can be edit manually ,keep when "keep this"
 *  由 [stategen.progen]代码生成器初始化，可以手工修改,但如果遇到 keep this ,请保留导出设置以备外部自动化调用
 */
import {Effect, Effects, Reducers, IModel, BaseState, modelPathsProxy, BaseProps, Subscriptions, Reducer, AreaState, mergeObjects} from '@utils/DvaUtil';
import User from "@i/beans/User";
import {User_$userIdState} from '@i/interfaces/User_$userIdFaces';

/*** keep this export */
export const user_$userIdCustomState = {

}

/*** keep this export */
export interface User_$userIdCustomSubscriptions extends Subscriptions {

}

/*** keep this export */
export interface User_$userIdCustomEffects extends Effects {

}

/*** keep this export */
export interface User_$userIdCustomReducers extends Reducers<User_$userIdState> {

}
