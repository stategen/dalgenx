/**
 *  Do not remove this unless you get business authorization.
 *  Dashboard
 *  init by [stategen.progen] ,can be edit manually ,keep when "keep this"
 *  由 [stategen.progen]代码生成器初始化，可以手工修改,但如果遇到 keep this ,请保留导出设置以备外部自动化调用
 */
import {Effect, Effects, Reducers, IModel, BaseState, modelPathsProxy, BaseProps, Subscriptions, Reducer, AreaState, mergeObjects} from '@utils/DvaUtil';
import {DashboardState} from '@i/interfaces/DashboardFaces';

/*** keep this export */
export const dashboardCustomState = {

}

/*** keep this export */
export interface DashboardCustomSubscriptions extends Subscriptions {

}

/*** keep this export */
export interface DashboardCustomEffects extends Effects {

}

/*** keep this export */
export interface DashboardCustomReducers extends Reducers<DashboardState> {

}
